import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 180,
    backgroundColor: '#1D1D1D',
    alignItems: 'center',
  },

  header: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 52,
  },
  
  textColor: {
    color: '#FFF',
    fontSize: 36,
    fontWeight: 'bold',
    marginRight: 8,
  }
});